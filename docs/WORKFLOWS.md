# Workflows

## Local Dev
- Install deps: `npm install`
- Start dev server: `npm run dev`
- Build: `npm run build`
- Preview/serve: `npm run start`

## Tests and Lint
- Test: Not configured.
- Watch: Not configured.
- Lint: `npm run lint`

## Deploy
- Primary deploy path: Next.js build and start (see `npm run build` / `npm run start`).
- Any environment-specific notes: README references Vercel as a deployment option, but no Vercel config is checked in.
