# Tasks

## TODO
- Provide a custom Soul narrative (beyond the default scaffold) and update the `soul` note.

## Explicit TODOs Found In Repo
- `README.md`
  - If you want a custom Soul narrative (beyond the default scaffold), provide the text and update the `soul` note.

## Backlog Ingestion
- Ingest `MISSION_CONTROL_STARTER_BACKLOG.md` into product planning and map to modules. (See `docs/BACKLOG.md`)

## Next Feature Sets (Draft)
- AI helpers (monthly review summarizer, automation architect, sermon outline helper).
  - Automation Architect is now implemented in `/ai`.
  - Sermon Builder module (series + outline editor) is next.
