# Contributing

## Local Setup
- Install deps: `npm install`
- Run dev: `npm run dev`

## Code Style
- Use TypeScript and Tailwind.
- Keep components under `src/components` when shared.

## Tests
- No automated tests configured in this repo.
